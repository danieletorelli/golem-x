package component: tweet-management;

interface tweet-api {
  // Post a new tweet
  post-tweet: func(user-id: string, content: string) -> result<string>; // tweet-id

  // Get tweets for a specific user
  get-user-tweets: func(user-id: string) -> result<list<string>>; // list of tweet-ids
}

world tweet-management {
  export tweet-api;
}
